#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
    int data[3];
    scanf("%d",&data[0]);
    scanf("%d",&data[1]);
    scanf("%d",&data[2]);
    for(int i = 0; i < 2; ++i)
    {
        if(data[i] < data[i+1])
        {
            int temp;
            
        }
    }
}
